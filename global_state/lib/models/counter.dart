import 'package:flutter/material.dart';

// Menyimpan value, label, dan warna
class Counter {
  int value;
  String label;
  Color color;

  Counter({this.value = 0, required this.label, required this.color});
}
